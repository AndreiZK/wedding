# Public site origin — no trailing slash.
# Drives canonical URLs, Open Graph / Twitter tags, robots.txt, sitemap.xml,
# and JSON-LD structured data. Falls back to http://localhost:3000 when unset.
NEXT_PUBLIC_SITE_URL=https://example.com

# Google Maps JavaScript API key for the venue map (location section).
# Public by design — restrict it by HTTP referrer in the Google Cloud Console
# (Maps JavaScript API). When unset, the map shows a styled placeholder + link.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# --- Server-only (no NEXT_PUBLIC_ prefix — never exposed to the browser) ---

# Optional upstream the /api/contact route forwards leads to (CRM / webhook).
# When unset, the route logs submissions server-side instead.
CONTACT_ENDPOINT=

# Telegram Bot API — guest form submissions from /api/preferences.
# Both must be set together or left empty (dev logs submissions instead).
#
# Setup:
# 1. Message @BotFather on Telegram → /newbot → copy the bot token.
# 2. Open a chat with your new bot and send any message.
# 3. Fetch updates to find your chat ID:
#    https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
#    → use message.chat.id from the JSON (a number, e.g. 123456789).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
